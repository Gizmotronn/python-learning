# http://copitosystem.com/python-dice-simulator/
import random
import time

print("Time to attack!")
time.sleep(2)
attackInput = input("Do you want to attack? (y/n)")

if attackInput == y:
    selected = random.randint(1,6) # the number selected by the "dice" // actually the random generator
